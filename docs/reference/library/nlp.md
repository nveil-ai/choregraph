# NLP Collection

NLP multi-label binarization functions. Converts text columns containing delimited labels
into binary occurrence matrices. Supports automatic language detection, lemmatization,
accent removal, and fuzzy matching to user-provided hint labels.

::: choregraph.collection.nlp
    options:
      members_order: source
      show_source: true
